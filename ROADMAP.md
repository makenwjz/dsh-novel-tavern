# 路线图（Roadmap）

> 版本规划与更新思路，随每版更新。当前版本：**v0.2（进行中）**。

## 更新思路（原则）

1. **卡驱动，不硬编码** —— 一切从卡发起的调用映射到 DSH，绝不为某张卡写死规则（周目切换=按条目名过滤就是例子）。
2. **"卡能跑起来"是最高优先级** —— 每张社区卡 = 一个测试用例；先补 SillyTavern 语义（开场白/变量/世界书切换），再做 UI 美化。
3. **先功能后美观，小步快发** —— 每版有清晰的"这版修了什么"。
4. **酒馆/小说保持解耦** —— 可单独安装、单独使用。
5. **安全底线** —— 卡 JS 只在沙箱跑、第三方脚本不执行危险操作。
6. **社区反馈闭环** —— issue 驱动的兼容性修复 + 持续更新。

## 版本路线

| 版本 | 主题 | 核心内容 |
|---|---|---|
| v0.1 | 已发布 | 基础小说/酒馆、微信式聊天、开场白显示层加载、卡正则渲染（沙箱 iframe）、卡桥接（周目/世界书切换）、注入预算保护 |
| **v0.2** | **P0 语义补全（当前）** | 见下方 P0 清单 |
| v0.3 | 体验打磨 | 开场白历史进阶、流式回复、卡内图片资源、多角色会话、深色适配 |
| v0.4 | 差异化 | 多 agent 角色扮演/编辑团队（agent-teams）、vision/browser 联动 |
| v0.5 | 生态 | npm 发布、插件市场上架、兼容性测试集、模板库、多作品管理、英文版 |

## v0.2 P0 清单（酒馆）

- [x] **开场白真正写入会话历史** —— 首次发送消息时把所选开场白写入 session 日志（第一条 assistant 消息），模型从它续写；prompt 注入端检测到开场已写就跳过强制 first_mes（SillyTavern 行为）。
- [x] **promptOnly 正则脚本执行** —— 卡里作用于 prompt 的脚本（变量更新对 AI 不可见、屏蔽思维链复读等）在注入端应用。
- [x] **MVU 变量系统** —— 会话级 MVU 状态（绑定存储 + 注入 `## 角色状态`）；桥接 `replaceMvuData` 真实更新；模型 `<json_patch>` 块自动回放（开场白初始化 + 每轮更新）。
- [x] **世界书条目手动编辑** —— 资料库每条世界书可独立启用/禁用，持久化到存储文件（SillyTavern 书编辑器行为）；卡前端会话级覆盖在此基础上生效。

## v0.2 P0 清单（小说）

- [x] **写作上下文自动注入** —— `chapter_context` 工具：一次调用返回本章知识控制、折叠的世界状态、canon 设定、待兑现伏笔、上下章正文。
- [x] **正文一致性检查** —— `manuscript_check`：返回草稿 + 提及的已知主体 + 世界/lore/伏笔，模型据以核对 canon、时间线、角色状态冲突。
- [x] **正文↔账本自动同步** —— `manuscript_scan`：扫描草稿识别伏笔信号（建议埋设）、应推进/兑现的伏笔、提及的主体。
- [x] **章节写作工作流** —— `chapter_workflow`：完整写作简报 + 六步计划（审阅→规划→写→lint→查→同步账本）。

## 未来方向（P1/P2 摘选）

- 流式回复（打字机效果 + 实时工具行）
- 卡内图片资源（表情/背景）进渲染页
- 多角色会话（说话人标注、按角色换头像）
- 深色/主题适配、大 HTML 懒加载
- 多 agent 扮演（每个 NPC 一个 agent）与小说编辑团队
- vision-toolkit / browser / genui 联动
- 小说↔酒馆设定互通（角色卡/世界书 ↔ canon 双向导入）
- 时间线拖动条、场景卡片看板、主体关系图
- npm 发布、插件市场上架、兼容性测试集、模板库、多作品管理
