/** Host loader entry for the novel workspace tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the novel workspace tab. */
export function apply(): void {}
